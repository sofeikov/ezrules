FROM node:20-alpine AS builder
WORKDIR /app
COPY ezrules/frontend/package*.json ./
RUN npm ci
COPY ezrules/frontend/ .
RUN npm run build

FROM nginx:alpine
COPY --from=builder /app/dist/browser /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf
EXPOSE 80
